<section
  class="scrub w-full relative grid place-items-center content-center text-center uppercase font-black"
>
  <h1 class="text-[clamp(2rem,8vw_+_1rem,12rem)]">101 proyectos javascript</h1>
  <a
    href="#main"
    class="absolute right-10 bottom-5 font-bold text-[5rem] hover:scale-125 hover:-rotate-12 active:rotate-12 font-sans"
    >↯<span class="sr-only">Go down</span></a
  >
</section>

<style>
  @property --scroll {
    initial-value: 0;
    inherits: true;
    syntax: "<number>";
  }
  @property --scrub {
    initial-value: 0;
    inherits: true;
    syntax: "<number>";
  }

  .scrub {
    height: min(calc(100vh - 60px), 980px);
    animation: scrub linear both;
    animation-timeline: scroll();
  }

  @keyframes scrub {
    100% {
      --scroll: 100;
      --scrub: 100;
    }
  }

  h1,
  a {
    --velocity: calc(var(--scroll) - var(--scrub));
    transition:
      --scrub 0.25s ease-in,
      transform 150ms;
    text-shadow:
      0 calc(var(--velocity) * 0.3px) var(--p-blue),
      0 calc(var(--velocity) * 0.6px) var(--p-green),
      0 calc(var(--velocity) * 0.9px) var(--p-yellow),
      0 calc(var(--velocity) * 1.2px) var(--p-orange),
      0 calc(var(--velocity) * 1.5px) var(--p-red);
    margin: 1rem;
  }
</style>
